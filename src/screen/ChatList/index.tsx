import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const ChatList = () => {
  return (
    <SafeAreaView>
      <Text>ChatList</Text>
    </SafeAreaView>
  );
};

export default ChatList;
